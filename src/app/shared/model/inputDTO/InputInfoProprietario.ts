export class InputInfoProprietario {
    nome: string
    cognome: string
    numDipendenti: number

    constructor(nome: string, cognome: string, numDipendenti: number) {
        this.nome = nome
        this.cognome = cognome
        this.numDipendenti = numDipendenti
    }
}